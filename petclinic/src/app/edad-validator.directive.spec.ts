import { EdadValidatorDirective } from './edad-validator.directive';

describe('EdadValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EdadValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
